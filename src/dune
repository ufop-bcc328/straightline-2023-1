(executables
 (names
   test_maxargs
   driver)
 (libraries
   printbox
   printbox-text)
 (preprocess
  (staged_pps
    ppx_import
    ppx_deriving.show))
 (flags
  (:standard -warn-error -A)))

(ocamllex
 (modules lexer))

(menhir
 (modules parser))
